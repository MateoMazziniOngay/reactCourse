// Selectores del DOM

// querySelector: se usa para retornar maximo un elemento
const heading = document.querySelector('h2')

console.log(heading)
console.log(heading.textContent)

// querySelectorAll: se usa para retornar todos los elementos que coincidan en el selector
const enlaces = document.querySelectorAll('.navegacion a')

console.log(enlaces)