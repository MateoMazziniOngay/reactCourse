// Variables let

// Es un lenguaje dinámico, una variable puede tener diferentes tipos de dato, porque el tipo de dato se almacena en el valor de la variable y no en
// la variable como tal

// Las variables con let se pueden reasignar, cambiando el tipo de valores, a su vez pueden inicializarse sin ningún valor.

// Se usa camelCase para escribir los nombres de las variables let.
